/** Device preference shared by the pre-paint script and interactive shell. */
// Existing browser preference key, extracted from shell.tsx. Keep it compatible
// with saved preferences until the storage namespace migration is implemented.
export const SIDEBAR_STORAGE_KEY = "majorana.sidebar-collapsed.v1";
